JWT_SECRET=replace_with_a_strong_random_secret
RABBITMQ_URL=amqp://USER:PASSWORD@localhost:5672
DB_HOST=127.0.0.1
DB_USER=postgres
DB_PASSWORD=your_db_password
DB_NAME=tasks_db

# Email / SMTP — leave empty in development to use Ethereal (auto-generated test account)
# For Gmail: SMTP_HOST=smtp.gmail.com  SMTP_PORT=587  SMTP_SECURE=false
# For production (SendGrid): SMTP_HOST=smtp.sendgrid.net  SMTP_PORT=587
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Task Manager <noreply@taskmanager.dev>"
